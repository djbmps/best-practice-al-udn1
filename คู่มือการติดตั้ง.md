# คู่มือการติดตั้งระบบส่งผลงาน Best Practice Active Learning
### สำนักงานเขตพื้นที่การศึกษาประถมศึกษาอุดรธานี เขต 1

---

## ไฟล์ในชุดนี้

| ไฟล์ | หน้าที่ |
|---|---|
| `index.html` | หน้าเว็บสำหรับ **ครูผู้สอน** ใช้ส่งผลงาน |
| `admin.html` | หน้าเว็บสำหรับ **ผู้ดูแลระบบ** ดู/ค้นหา/ส่งออกข้อมูล |
| `Code.gs` | โค้ดหลังบ้าน สำหรับ Google Apps Script |
| `schools.json` | ข้อมูลกลุ่มโรงเรียน 20 กลุ่ม / 214 โรงเรียน (สำรองไว้) |

> ข้อมูลโรงเรียนถูกฝังอยู่ในไฟล์ `index.html` เรียบร้อยแล้ว ไม่ต้องอัปโหลด `schools.json` ขึ้นเว็บ

---

## ขั้นตอนที่ 1 — เตรียม Google Sheet

1. เข้า [sheets.google.com](https://sheets.google.com) → สร้างสเปรดชีตใหม่
2. ตั้งชื่อ เช่น **“ผลงาน Best Practice AL 2569”**
3. คัดลอก **Spreadsheet ID** จาก URL — คือข้อความระหว่าง `/d/` กับ `/edit`

```
https://docs.google.com/spreadsheets/d/1AbCdEfGhIjK...XyZ/edit
                                       └────── นี่คือ ID ──────┘
```

> ไม่ต้องสร้างหัวตารางเอง ระบบจะสร้างแผ่นงานชื่อ **“ผลงาน”** พร้อมหัวตารางให้อัตโนมัติในการส่งครั้งแรก

---

## ขั้นตอนที่ 2 — เตรียมโฟลเดอร์ Google Drive

1. เข้า [drive.google.com](https://drive.google.com) → สร้างโฟลเดอร์ใหม่ เช่น **“แผนการสอน Best Practice AL 2569”**
2. เปิดโฟลเดอร์ แล้วคัดลอก **Folder ID** จาก URL — คือข้อความหลัง `/folders/`

```
https://drive.google.com/drive/folders/1XyZaBcDeFgH...123
                                       └────── นี่คือ ID ──────┘
```

---

## ขั้นตอนที่ 3 — สร้าง Google Apps Script

1. เข้า [script.google.com](https://script.google.com) → คลิก **โครงการใหม่ (New project)**
2. ตั้งชื่อโครงการ เช่น **“BestPractice-AL-UDN1”**
3. ลบโค้ดตัวอย่างในไฟล์ `Code.gs` ทิ้งทั้งหมด แล้ว **คัดลอกเนื้อหาจากไฟล์ `Code.gs` ในชุดนี้ไปวางแทน**
4. แก้ค่า 3 บรรทัดบนสุดของโค้ด:

```javascript
const SHEET_ID   = 'วาง Spreadsheet ID จากขั้นตอนที่ 1';
const FOLDER_ID  = 'วาง Folder ID จากขั้นตอนที่ 2';
const ADMIN_PASS = 'ตั้งรหัสผ่านผู้ดูแลระบบของท่านเอง';
```

5. กด **บันทึก** (ไอคอนแผ่นดิสก์ หรือ Ctrl+S)

### ทดสอบการตั้งค่า
6. ที่แถบด้านบน เลือกฟังก์ชัน **`ทดสอบการตั้งค่า`** → กด **เรียกใช้ (Run)**
7. ครั้งแรกจะมีหน้าต่างขออนุญาต → **ตรวจสอบสิทธิ์ → เลือกบัญชี Google ของท่าน**
   → หากขึ้นเตือน “Google ยังไม่ได้ยืนยันแอปนี้” ให้กด **ขั้นสูง (Advanced)** → **ไปที่ BestPractice-AL-UDN1 (ไม่ปลอดภัย)** → **อนุญาต**
8. ดูผลที่แถบ **บันทึกการดำเนินการ (Execution log)** ด้านล่าง ต้องขึ้น ✅ ทั้งสองบรรทัด

---

## ขั้นตอนที่ 4 — เผยแพร่เป็น Web App

1. กดปุ่ม **ทำให้ใช้งานได้ (Deploy)** มุมขวาบน → **การทำให้ใช้งานได้ใหม่ (New deployment)**
2. คลิกไอคอนฟันเฟือง ⚙️ ข้าง “เลือกประเภท” → เลือก **เว็บแอป (Web app)**
3. ตั้งค่าดังนี้ (**สำคัญมาก**)

   | ช่อง | ค่าที่ต้องเลือก |
   |---|---|
   | คำอธิบาย | `เวอร์ชัน 1` |
   | ดำเนินการในฐานะ (Execute as) | **ฉัน (Me)** |
   | ผู้ที่มีสิทธิ์เข้าถึง (Who has access) | **ทุกคน (Anyone)** |

4. กด **ทำให้ใช้งานได้ (Deploy)** → คัดลอก **URL ของเว็บแอป** ที่ลงท้ายด้วย `/exec`

```
https://script.google.com/macros/s/AKfycb....../exec
```

> ⚠️ ต้องเลือก **“ทุกคน (Anyone)”** เท่านั้น มิฉะนั้นครูที่ไม่ได้ล็อกอินจะส่งผลงานไม่ได้

---

## ขั้นตอนที่ 5 — ใส่ URL ลงในไฟล์เว็บ

เปิดไฟล์ **`index.html`** ด้วย Notepad / TextEdit / VS Code แล้วมองหาบรรทัดนี้ (อยู่ในส่วน `<script>` ท้ายไฟล์)

```javascript
const API_URL = "";
```

เปลี่ยนเป็น

```javascript
const API_URL = "https://script.google.com/macros/s/AKfycb....../exec";
```

**ทำแบบเดียวกันกับไฟล์ `admin.html`** (ใช้ URL เดียวกัน) แล้วบันทึกทั้งสองไฟล์

---

## ขั้นตอนที่ 6 — นำเว็บขึ้นออนไลน์

เลือกวิธีใดวิธีหนึ่ง

**ก. เว็บไซต์ของเขตพื้นที่ฯ (แนะนำ)**
อัปโหลด `index.html` และ `admin.html` ขึ้นโฮสติ้งของเขต เช่นโฟลเดอร์ `bestpractice/`
→ ครูเข้าที่ `https://เว็บของเขต/bestpractice/index.html`

**ข. Google Sites**
สร้างหน้าใหม่ → แทรก → **ฝัง (Embed)** → เลือก **โค้ดฝัง** → วางเนื้อหาไฟล์ทั้งไฟล์

**ค. GitHub Pages / Netlify (ฟรี)**
ลากไฟล์ทั้งสองขึ้น [netlify.com/drop](https://app.netlify.com/drop) จะได้ลิงก์ทันที

> หน้า `admin.html` ควรเก็บลิงก์ไว้ใช้เฉพาะภายใน ไม่ต้องประกาศให้ครูทราบ

---

## ขั้นตอนที่ 7 — ทดสอบระบบ

1. เปิด `index.html` → กรอกข้อมูลทดสอบให้ครบ → แนบ PDF สั้น ๆ → กด **ส่งผลงาน**
2. ต้องขึ้นหน้า ✅ พร้อม **รหัสอ้างอิง** เช่น `AL69-0001`
3. เปิด Google Sheet → ต้องมีข้อมูล 1 แถว พร้อมลิงก์ไฟล์
4. เปิดโฟลเดอร์ Drive → ต้องมีไฟล์ PDF ชื่อขึ้นต้นด้วยรหัสอ้างอิง
5. เปิด `admin.html` → ใส่รหัสผ่านที่ตั้งไว้ (`ADMIN_PASS`) → ต้องเห็นรายการที่เพิ่งส่ง
6. ลบแถวทดสอบออกจากชีตและไฟล์ทดสอบใน Drive ก่อนเปิดใช้งานจริง

---

## สิ่งที่ระบบมีให้

**หน้าครูผู้สอน (`index.html`)**

- กลุ่มสาระการเรียนรู้ **9 รายการ** — 8 กลุ่มสาระ + การจัดการเรียนรู้เศรษฐกิจพอเพียง
- ช่องกรอก: คำนำหน้า (มีตัวเลือก “อื่น ๆ” ให้พิมพ์เอง), ชื่อ-สกุล, ตำแหน่ง, วิทยฐานะ, เบอร์โทรศัพท์ (จัดรูปแบบอัตโนมัติ), ID Line
- เลือก **กลุ่มโรงเรียน** แล้วรายชื่อ **โรงเรียนในกลุ่ม** จะแสดงให้เลือกอัตโนมัติ (20 กลุ่ม / 214 โรงเรียน)
- ชื่อผลงาน, ระดับชั้น, ลิงก์คลิปวิดีโอ
- อัปโหลดแผนการจัดการเรียนรู้ (ลากวางได้) — ตรวจชนิดไฟล์และขนาดทั้งฝั่งเว็บและฝั่งเซิร์ฟเวอร์
- คำรับรองผลงาน + แถบแสดงความคืบหน้า + รหัสอ้างอิงเมื่อส่งสำเร็จ
- ใช้งานได้ทั้งบนคอมพิวเตอร์ แท็บเล็ต และมือถือ

**หน้าผู้ดูแลระบบ (`admin.html`)**

- เข้าระบบด้วยรหัสผ่าน
- การ์ดสรุป: จำนวนผลงาน / โรงเรียนที่ส่ง / กลุ่มโรงเรียน / กลุ่มสาระ
- ค้นหาข้อความ + กรองตามกลุ่มสาระ / กลุ่มโรงเรียน / ระดับชั้น
- คลิกหัวตารางเพื่อเรียงลำดับ
- เปิดไฟล์แผนการสอนและคลิปวิดีโอได้ในคลิกเดียว
- **ส่งออก CSV** เปิดใน Excel ได้ทันที (รองรับภาษาไทย)

---

## การแก้ไขในภายหลัง

**เพิ่ม/แก้ไข/ลบรายชื่อโรงเรียน**
เปิด `index.html` หา `const SCHOOLS = {` แล้วแก้รายชื่อในกลุ่มที่ต้องการ (รูปแบบ JSON ธรรมดา)

**เพิ่ม/แก้ไขกลุ่มสาระ**
เปิด `index.html` หา `const SUBJECTS = [` แล้วแก้ไขรายการ

**เปลี่ยนรหัสผ่านผู้ดูแล**
แก้ `ADMIN_PASS` ใน Apps Script → **Deploy → จัดการการทำให้ใช้งานได้ → แก้ไข ✏️ → เวอร์ชัน: ใหม่ → ทำให้ใช้งานได้**

> ⚠️ ทุกครั้งที่แก้ไข `Code.gs` ต้อง **Deploy เวอร์ชันใหม่** เสมอ มิฉะนั้นการแก้ไขจะยังไม่มีผล
> (ใช้ “แก้ไขการทำให้ใช้งานได้เดิม” เพื่อให้ URL คงเดิม ไม่ต้องแก้ไฟล์ HTML ใหม่)

---

## ปัญหาที่พบบ่อย

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| กดส่งแล้วขึ้น “เกิดข้อผิดพลาดในการเชื่อมต่อ” | ยังไม่ได้ใส่ `API_URL` หรือ Deploy ไม่ได้ตั้ง “ทุกคน (Anyone)” |
| ขึ้น “ข้อมูลไม่ครบถ้วน” | มีช่องบังคับที่เว้นว่าง ลองเลื่อนหาช่องที่มีกรอบสีแดง |
| ไฟล์ใหญ่เกิน 10 MB | บีบอัด PDF ที่ [ilovepdf.com/compress_pdf](https://www.ilovepdf.com/compress_pdf) |
| หน้า admin ขึ้น “รหัสผ่านไม่ถูกต้อง” | รหัสไม่ตรงกับ `ADMIN_PASS` หรือแก้แล้วยังไม่ได้ Deploy เวอร์ชันใหม่ |
| กรรมการเปิดไฟล์แผนการสอนไม่ได้ | องค์กรปิดการแชร์สาธารณะ — เปิดโฟลเดอร์ Drive แล้วแชร์ให้กรรมการโดยตรง |
| ส่งผลงานพร้อมกันหลายคน | ระบบใช้ LockService ป้องกันข้อมูลทับกันอยู่แล้ว |

---

## แหล่งข้อมูลโรงเรียน

ระบบ **BIG DATA สพป.อุดรธานี เขต 1** ปีการศึกษา **2569 ภาคเรียนที่ 1**
`https://bigdata.udn1.info/tableSchoolmis.php?op=2.1&year=2569-1`
รวม **20 กลุ่มโรงเรียน / 214 โรงเรียน**

| อำเภอ | กลุ่ม | โรงเรียน |
|---|---:|---:|
| เมืองอุดรธานี | 9 | 97 |
| หนองวัวซอ | 3 | 29 |
| เพ็ญ | 6 | 66 |
| สร้างคอม | 2 | 22 |
| **รวม** | **20** | **214** |
